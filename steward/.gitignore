# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
.env.local
.env.*.local
# ...but DO commit the example templates (no secrets)
!.env.example
!api/.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local Claude Code instructions (not for version control)
CLAUDE.md

# Playwright and Demo Recordings
/demo-media/
/tests/
playwright.config.ts
record-demo.bat
stitch-videos.js
generate-token.js
state.json

# Agent and AI artifacts
/.agents/
/.agent/
/.claude/
skills-lock.json

# Local Backend / DB
/api/steward.db
/api/get_ids*.py
/api/output_ids.json
/api/seed.py
/api/seed_output.txt

# internal audit / launch-prep docs (not for production or the public repo)
PRE-LAUNCH-GUIDE.md
DEPLOYMENT.md
